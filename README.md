# Music Favorites Page

A simple webpage that displays favorite songs (mocked from YouTube Music liked songs).

## Features
- HTML + CSS for layout and styling
- JavaScript loads songs dynamically from `songs.json`
- Dockerfile to run the site in a container

## Run Locally
1. Clone the repo:
   ```bash
   git clone https://github.com/your-username/music-favorites-page.git
   cd music-favorites-page
